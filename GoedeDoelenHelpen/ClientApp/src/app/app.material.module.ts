import { NgModule } from '@angular/core';
import { MatToolbarModule, MatMenu, MatMenuItem, MatButton } from '@angular/material';

@NgModule({
  imports: [
    MatToolbarModule
    // MatMenu,
    // MatMenuItem,
    // MatButton
  ],
  exports: [
    MatToolbarModule
    // MatMenu,
    // MatMenuItem,
    // MatButton
  ]
})
export class AppMaterialModule { }
